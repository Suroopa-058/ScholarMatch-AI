import { useState } from "react";
import { ArrowRight, GraduationCap, LockKeyhole, Mail, UserRound } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { api } from "../services/api";
import type { ApiError } from "../types/api";
import { saveAuth } from "../utils/auth";

export default function Auth({ mode }: { mode: "login" | "signup" }) { const navigate = useNavigate(); const isSignup = mode === "signup"; const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(null); if (isSignup && name.trim().length < 2) { setError("Please enter your name."); return; } if (password.length < 8) { setError("Your password must be at least 8 characters."); return; } setLoading(true); try { const result = isSignup ? await api.signup(name, email, password) : await api.login(email, password); saveAuth(result); navigate("/profile"); } catch (caught) { setError((caught as ApiError).message); } finally { setLoading(false); } }; return <section className="auth-shell"><div className="auth-intro"><Link to="/" className="flex items-center gap-3 text-white"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-white/15"><GraduationCap size={22} /></span><span className="font-display text-xl font-semibold">ScholarMatch AI</span></Link><div className="mt-auto"><p className="eyebrow !text-[var(--color-brass)]">A clearer path forward</p><h1 className="mt-4 font-display text-4xl font-semibold leading-tight">Find opportunities that fit your potential.</h1><p className="mt-5 max-w-md leading-relaxed text-white/70">Build your profile, discover tailored scholarship matches, and understand what guided every result.</p></div><p className="mt-12 text-sm text-white/55">Academic recommendation prototype</p></div><div className="auth-form-wrap"><Link to="/" className="text-sm font-semibold text-[var(--color-blue)]">← Back to home</Link><div className="mt-12 max-w-md"><p className="eyebrow">{isSignup ? "Create account" : "Welcome back"}</p><h2 className="mt-3 font-display text-4xl font-semibold text-[var(--color-navy)]">{isSignup ? "Start your journey." : "Sign in to continue."}</h2><p className="mt-3 text-[var(--color-muted)]">{isSignup ? "Create an account to save your access on this device." : "Enter your account details to continue."}</p><form className="mt-9 space-y-5" onSubmit={submit}>{isSignup && <label className="field"><span>Full name</span><span className="input-with-icon"><UserRound size={17} /><input value={name} onChange={(e) => setName(e.target.value)} placeholder="Your name" autoComplete="name" required /></span></label>}<label className="field"><span>Email address</span><span className="input-with-icon"><Mail size={17} /><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email" required /></span></label><label className="field"><span>Password</span><span className="input-with-icon"><LockKeyhole size={17} /><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="At least 8 characters" autoComplete={isSignup ? "new-password" : "current-password"} required /></span></label>{error && <p role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="button-primary w-full justify-center">{loading ? "Please wait…" : isSignup ? "Create account" : "Sign in"}<ArrowRight size={16} /></button></form><p className="mt-7 text-center text-sm text-[var(--color-muted)]">{isSignup ? "Already have an account?" : "New to ScholarMatch AI?"} <Link className="font-semibold text-[var(--color-blue)]" to={isSignup ? "/login" : "/signup"}>{isSignup ? "Sign in" : "Create an account"}</Link></p></div></div></section>; }
