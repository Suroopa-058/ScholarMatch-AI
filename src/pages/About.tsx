const pipeline = ["Student Profile", "Feature Engineering", "SBERT Semantic Similarity", "XGBoost + Random Forest + LightGBM + CatBoost", "Ensemble Score", "Top-K Ranking", "SHAP Explanation"];
export default function About() { return <section className="mx-auto max-w-6xl px-5 py-12 sm:px-6 sm:py-16"><p className="eyebrow">About the project</p><h1 className="page-title">Scholarships, made easier to navigate.</h1><div className="mt-10 grid gap-6 md:grid-cols-2"><article className="panel"><h2 className="section-title">What is ScholarMatch AI?</h2><p className="mt-4 leading-relaxed text-[var(--color-muted)]">ScholarMatch AI is a final-year project that helps students compare scholarship opportunities using profile-aware recommendations instead of manual searching alone.</p></article><article className="panel"><h2 className="section-title">Our approach</h2><p className="mt-4 leading-relaxed text-[var(--color-muted)]">The system combines semantic matching with a multi-model machine-learning ensemble to score and rank scholarship compatibility, then exposes the factors behind each recommendation.</p></article></div><section className="mt-10 rounded-3xl bg-[var(--color-navy)] p-7 text-white sm:p-10"><p className="eyebrow !text-[var(--color-brass)]">AI pipeline</p><h2 className="mt-3 font-display text-3xl font-semibold">From profile to explainable result</h2><ol className="mt-8 grid gap-3 sm:grid-cols-2">{pipeline.map((step, index) => <li key={step} className="flex items-center gap-3 rounded-xl border border-white/15 p-4"><span className="font-mono text-xs text-[var(--color-brass)]">{String(index + 1).padStart(2, "0")}</span><span className="text-sm font-medium">{step}</span></li>)}</ol></section></section>; }
